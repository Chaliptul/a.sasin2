#include <stdio.h>
int main(){
	float sum = 0;
	int i;
	float DensityA;
	float DensityW;
	int beaker = 5000; //ปริมาตรบีกเกอร์ที่ใช้เป็นภาชนะสำหรับใส่น้ำและแอปเปิดล 5,000 ml
	int count = 1;
	int w = 250; //ใส่น้ำครั้งละ 2
	printf("Enter the density of an apple : ");
	scanf ("%f", &DensityA);






















   //for (i = 1; i <= 5; i++) {
       // printf("ค่าปัจจุบันคือ: %d\n", i); // แสดงค่าปัจจุบันของ i
   // }
//if ( DensityA < beaker)
//  printf(" the snail can escape \n");
// else
     //   printf("the snail can not escape \n");
    //return 0;

	}