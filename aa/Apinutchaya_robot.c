#include<stdio.h>
int main(){
		int move ();
		int n,i;
		int x = 0,y = 0;
		int count [7] = {0};

		printf("จำนวนครั้ง : ");
		scanf("%d",&move);

		printf ("ป้อนเส้นทาง 1-8:\n");

		for (i=0;i<n ;i++){
			scanf("%d",&move ());
		if (move() <1 || move() >8){
			printf("เลข1-8เท่านั้น\n");
			return0;
			}
		count [move[i]]++;
		 for (int i = 1; i <= n; i++) {
			 if (move() == 1) {
				 }

}